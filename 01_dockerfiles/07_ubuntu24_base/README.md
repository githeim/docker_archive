Ubuntu 24 base docker image
=================

for general purpose ubuntu 24 development environment

bd.sh : image build

r_gpu.sh : start docker container with nvidia runtime
r.sh     : start docker container

